package com.vibe.fundsmith.model;

/**
 * Enum representing the type of journal entry
 */
public enum JournalType {
    TRADE_DATE,     // Initial recognition on trade date
    SETTLEMENT_DATE // Recognition on settlement date
}